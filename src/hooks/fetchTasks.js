import React from 'react';

export const fetchTasks = () => {
  // tasks reducer

  // see if useContext call can be put into a funtion that is then called inside useEffect w/ no error
  // const getTasks = () => {
  //   const { state } = useContext(Store);
  //   setTasks(state[0].tasks);
  // };
  // useEffect(() => {
  //   getTasks();
  // }, [tasks]);

  //********** REDUX will likely be easier to implement multiple reducers  */
  //   return ({tasks});

  return {};
};
